[build]
  command = "npm run build"
  publish = "dist"

# Ensure TypeScript is available if needed
[build.environment]
  NODE_VERSION = "18"

# SPA redirect so client-side routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
